# Report: 001 customer-app に Vite HTTPS + /rpc proxy を追加

> Generated by dev-impl / 2026-05-22 / status: done

## 変更ファイル

- `apps/customer-app/vite.config.ts` (置換) — basicSsl + `server.https` + `/rpc`, `/friendbot` proxy
- `apps/customer-app/.env` — `VITE_RPC_URL` を `https://localhost:5173/rpc` に書き換え、ヘッダコメント更新
- `apps/customer-app/package.json` — `@vitejs/plugin-basic-ssl@^2.3.0` を devDependencies に追加

## 検証結果

| 項目 | 結果 |
|------|------|
| `pnpm --filter customer-app dev` が `https://localhost:5173/` で起動 | ✅ Vite ログに該当 URL 出力 |
| `POST https://localhost:5173/rpc` (`getNetwork`) が 200 + 正常 JSON-RPC | ✅ `{"passphrase":"Standalone Network ; February 2017",...}` 受信 |
| 外部 `scripts/rpc-https-proxy.mjs` 不要 | ✅ 起動せずに proxy 経由疎通成立 |
| basicSsl の cert 出力先 | `node_modules/.vite/basic-ssl/_cert.pem` (gitignore 二重カバー：`node_modules/` と `*.pem`) |
| `/friendbot` proxy | proxy 自体は機能（upstream の 502 を素通し）。localnet quickstart の friendbot エンドポイント挙動は本タスク対象外 |

LoginPage → passkey 新規作成→ホーム到達の手動 E2E（ホスト Chrome）は本環境で実行不可（ヘッドレス／コンテナ内）。ホストブラウザでの確認は引き続き手動で必要。

## 確信度レポート

### 🔵 前工程指示（レビュー低優先）

- `apps/customer-app/vite.config.ts` — タスクファイル Interfaces セクションの雛形をほぼそのまま採用
- `apps/customer-app/.env` の `VITE_RPC_URL` 値 — Plan の `kit rpcUrl の絶対 URL 要求` 検証結果に準拠

### 🟡 妥当な推測（要確認）

- `/friendbot` proxy をデフォルト追加 — タスクの実装例には含まれていたが「予防的」コメント付き。実用時に有効化される。問題なし。
- `VITE_RPC_PROXY_URL` も `https://localhost:5173/rpc` に揃えた — 旧コード経路の互換性目的。未使用なら Task 003 で削除候補。

### 🔴 AI 推論補完（人間の確認必須）

- なし

## Next

- Task 002 (staff-app 側の同型化) は 001 と独立に同様の手順で着手可能。
- Task 003 で E2E / sa-harness 経路への影響範囲評価が必要。
