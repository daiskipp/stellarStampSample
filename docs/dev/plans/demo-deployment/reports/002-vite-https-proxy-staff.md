# Report: 002 staff-app に Vite HTTPS + /rpc proxy を追加

> Generated by dev-impl / 2026-05-22 / status: done

## 変更ファイル

- `apps/staff-app/vite.config.ts` (置換) — basicSsl + `server.https` + `/rpc`, `/friendbot` proxy
- `apps/staff-app/.env` — `VITE_RPC_URL` を `https://localhost:5174/rpc` に書き換え、ヘッダコメント更新（HQ SA / staff rule ids 等は **保持**）
- `apps/staff-app/package.json` — `@vitejs/plugin-basic-ssl@^2.3.0` を devDependencies に追加

## 検証結果

| 項目 | 結果 |
|------|------|
| `pnpm --filter staff-app dev` が `https://localhost:5174/` で起動 | ✅ Vite ログ確認 |
| `POST https://localhost:5174/rpc` (`getNetwork`) が 200 + 正常 JSON-RPC | ✅ passphrase 確認 |
| 外部 `scripts/rpc-https-proxy.mjs` 起動不要 | ✅ proxy 未起動で疎通 |
| HQ SA / staff rule ids / `VITE_HQ_STAFF_BENEFITS_RULE_ID` 等の保全 | ✅ 触らず維持 |

staff sign-in → issue (U2 case-C) のホスト Chrome 経由 E2E 検証はコンテナ内では実行不可。手動検証はユーザ側で必要。

## 確信度レポート

### 🔵 前工程指示（レビュー低優先）

- `apps/staff-app/vite.config.ts` — 001 と同型、タスクの Interfaces 雛形どおり
- `apps/staff-app/.env` の `VITE_RPC_URL` — Task 002 で `https://localhost:5174/rpc` 指定

### 🟡 妥当な推測（要確認）

- `VITE_RPC_PROXY_URL` も同じ URL に揃え — 001 と同じく Task 003 で扱う互換シム

### 🔴 AI 推論補完（人間の確認必須）

- なし

## Next

- Task 003 (E2E / sa-harness 経路の env 切り分け) でこの dev 経路と既存 pipeline 経路の併存を整える。
