# Report: 004 scripts/deploy-testnet.sh を wasm32v1-none + 新 env スキーマに更新

> Generated by dev-impl / 2026-05-22 / status: done

## 変更ファイル

- `scripts/deploy-testnet.sh` (リライト)
- `.gitignore` — `.env.testnet` と `.env.testnet.bak` を追加（実行時生成・コミット禁止）

## 主な改修点

| カテゴリ | 旧 → 新 |
|---|---|
| ビルドターゲット | `cargo build --release --target wasm32-unknown-unknown` → `stellar contract build` (`target/wasm32v1-none/release/`) |
| Pre-flight | なし → `stellar` / `cargo` を `command -v` で検出、無い場合は早期 `ERROR + exit 1` |
| 既存 `.env.testnet` の扱い | 無条件上書き → `.env.testnet.bak` に退避してから新規生成 |
| `.env.testnet` テンプレ | rule ids 系が `VITE_HQ_STAFF_CONTEXT_RULE_ID` のみ → 完全形（`VITE_HQ_STAFF_RULE_IDS`, `VITE_HQ_STAFF_BENEFITS_RULE_ID`, `VITE_DEMO_CUSTOMER_SA` 追加） |
| friendbot fund | 初回のみ → `\|\| true` で常時 idempotent 化 |
| 完了ハンドオフ | `initialize-contracts.sh` (legacy) → `generate-bindings.sh` + `sa-setup-testnet.mjs` (Task 005 で実装予定) |

OZ Testnet infra アドレス (`VITE_ACCOUNT_WASM_HASH`, verifier 群, threshold/spending-limit/weighted policies) は旧スクリプトの値をそのまま流用（smart-account-kit demo `.env.example` 由来）。

## 検証結果

| 項目 | 結果 |
|------|------|
| `bash -n scripts/deploy-testnet.sh` | ✅ 構文 OK |
| Pre-flight: stellar 無し → 即エラー | ✅ `ERROR: stellar CLI not found in PATH` |
| Pre-flight: cargo 無し → 即エラー | ✅ `ERROR: cargo not found in PATH (stellar contract build needs it)` |
| `.env.testnet` gitignore | ✅ `git check-ignore` で `.gitignore:39:.env.testnet` 一致 |
| 実行時 Testnet deploy | ⚠️ **本セッションでは実行せず** — Testnet 鍵生成・friendbot 利用は副作用が外部に及ぶため、ユーザ側で初回実行する想定 |
| 生成 .env.testnet の Vite 取り込み | ⚠️ 未確認 — `VITE_NETWORK_PASSPHRASE` が unquoted (`Test SDF Network ; September 2015`) で旧 deploy-testnet.sh / localnet と同じフォーマット、Vite (dotenv) 経由で読めるはず |

## 確信度レポート

### 🔵 前工程指示（レビュー低優先）

- wasm target を `wasm32v1-none` + `stellar contract build` に切替 — CLAUDE.md / context.md / 既存 `deploy-localnet.sh` と整合
- `.env.testnet` テンプレ完全化 — タスク雛形どおり全キー記載
- identity 名 `dicekey-admin` 維持 — 既存ドキュメント整合

### 🟡 妥当な推測（要確認）

- pre-flight に `cargo` 検出を追加 — `stellar contract build` は内部で cargo を呼ぶため。タスクの「cargo / stellar CLI が無い場合は早期エラー」を素直に実装
- `stellar keys fund ... || true` — 既存 funded アカウントで失敗するため idempotency を優先
- `.env.testnet.bak` も gitignore に追加 — `.bak` が誤コミットされる事故を防ぐ
- `initialize-contracts.sh` への参照を削除し、`sa-setup-testnet.mjs` (Task 005) を案内 — Plan 全体の流れと整合させる選択

### 🔴 AI 推論補完（人間の確認必須）

- 実 Testnet での deploy 動作 — 鍵生成 + friendbot + 5 contracts upload + .env.testnet 書き出しの一連が想定どおり通るかはユーザ環境で要検証
- `VITE_NETWORK_PASSPHRASE=Test SDF Network ; September 2015` の unquoted 表記 — Vite dotenv パーサが `;` を含む値を素直に読むことを apps 起動時に要確認 (旧スクリプトでも同じ書き方だったので probably OK)

## Next

- Task 005 (`scripts/sa-setup-testnet.mjs`) で HQ SA を Testnet に作成し `.env.testnet` の HQ/staff キーを埋める。
- Task 006/007 で `.env.testnet` を CF Pages env vars に同期し、`VITE_RP_ID` を実 host に書換える。
