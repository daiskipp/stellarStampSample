# Report: 005 scripts/sa-setup-testnet.mjs を新規追加 (HQ SA bootstrap on Testnet)

> Generated by dev-impl / 2026-05-22 / status: done

## 変更ファイル

- `scripts/sa-setup-testnet.mjs` (新規, 318 lines)
- `.gitignore` — `tools/sa-harness/.env.testnet` と `tools/sa-harness/fixtures.testnet.json` を追加

`scripts/sa-setup.mjs`（localnet 用）と `tools/sa-harness/vite.config.ts` は **無変更**。

## アーキテクチャ判断

| 判断 | 理由 |
|------|------|
| 既存 `sa-setup.mjs` をベースに新規ファイル化 | CLAUDE.md ルール「localnet 既存運用を壊さない」を厳守 |
| harness 側 `.env.testnet` を runtime 生成 | `deploy-localnet.sh` が `tools/sa-harness/.env` を実行時生成する既存パターンと整合。テンプレファイル化は冗長 |
| `vite.config.ts` 無変更 | `envDir: "."` がすでに設定済み。`vite --mode testnet` で `.env.testnet` を `.env` に上書き読込する Vite 標準挙動が利用できる |
| HQ-rules-setup script 自身が harness Vite を spawn | `pnpm --filter sa-harness dev --mode testnet` をスクリプト内から起動し、終了時に kill。one-shot 化（sa-setup.mjs は外部起動前提だったが本スクリプトは self-contained）|
| RPC HTTPS proxy 不要 | Testnet RPC が公開 HTTPS のため kit がそのまま叩ける |
| Friendbot fund | `scripts/fund-localnet.mjs` と同じ `Keypair.fromRawEd25519Seed(hash("openzeppelin-smart-account-kit"))` でアドレス derive、最大 3 回 retry（429 は 5s sleep、それ以外 2s）|
| fixtures 出力先 | `tools/sa-harness/fixtures.testnet.json` で localnet 側 `fixtures.json` と分離 |

## フロー

```
deploy-testnet.sh  (Task 004)
  └─ .env.testnet (HQ keys blank)
       └─ sa-setup-testnet.mjs  (本タスク)
            ├─ fund kit deployer (friendbot Testnet)
            ├─ sync .env.testnet -> tools/sa-harness/.env.testnet
            ├─ spawn harness vite (--mode testnet)
            ├─ CDP virtual authenticator + page driver:
            │    createWallet HQ → setupStaffRules → initialize → wire
            │    → createWallet customer → reconnect HQ → issueStamp
            ├─ verify stamp_count / beans on-chain
            ├─ write back HQ SA + 4 rule ids + customer SA into .env.testnet
            └─ fixtures.testnet.json
```

## 検証結果

| 項目 | 結果 |
|------|------|
| `node --check scripts/sa-setup-testnet.mjs` | ✅ Syntax OK |
| `scripts/sa-setup.mjs` 差分 | ✅ 無変更 (git diff 空) |
| `tools/sa-harness/vite.config.ts` 差分 | ✅ 無変更 |
| `tools/sa-harness/.env.testnet` の gitignore | ✅ (`.gitignore` に追加済) |
| `tools/sa-harness/fixtures.testnet.json` の gitignore | ✅ |
| 実 Testnet bootstrap 実行 | ⚠️ **本セッションでは未実行** — Task 004 (deploy-testnet.sh) もユーザ側で要走、その後本スクリプトを走らせる前提 |

## 確信度レポート

### 🔵 前工程指示（レビュー低優先）

- 5-step bootstrap (HQ → rules → init → wire → customer → issue) — sa-setup.mjs 構造をそのまま踏襲
- `.env.testnet` の 5 keys (HQ_SMART_ACCOUNT / STAFF_RULE_IDS / STAFF_CONTEXT_RULE_ID / STAFF_BENEFITS_RULE_ID / DEMO_CUSTOMER_SA) を書き戻す — Task 004 の placeholder と対応
- localnet sa-setup.mjs を温存 — CLAUDE.md ルール厳守

### 🟡 妥当な推測（要確認）

- **harness Vite を script 内 spawn** — sa-setup.mjs は外部起動前提だが、Testnet 版ではユーザの混乱を避けるため self-contained を選択。終了時に kill する
- **`vite --mode testnet`** で `.env.testnet` を読込 — Vite 標準挙動。`tools/sa-harness/.env` の localnet 値は `.env.testnet` で完全 override される必要があり、project root `.env.testnet` を copyFileSync で wholesale コピーすることで保証
- **`fixtures.testnet.json`** に出力先を分離 — localnet `fixtures.json` を上書きしないため
- friendbot retry 戦略: 429 → 5s、他 → 2s、最大 3 試行 — タスク雛形の「5秒待って再試行」を素直に実装

### 🔴 AI 推論補完（人間の確認必須）

- 実 Testnet 環境での bootstrap 動作 — `initializeDicekey` / `wireContracts` / `issueStamp` の pollTransaction が Testnet の latency でデフォルト attempt 数内に完走するかは未検証。失敗時は再 deploy + 再 run で対処（Task 雛形通り）
- `pnpm --filter sa-harness dev --mode testnet` のオプション伝播 — pnpm が `dev` script (vite) に `--mode testnet` を素通しするかは pnpm version 依存。smoke-customer-app.mjs では同じパターン (`--mode test`) が動作していたため probable OK

## Next

- ユーザ側で:
  1. `bash scripts/deploy-testnet.sh`
  2. `bash scripts/generate-bindings.sh`
  3. `node scripts/sa-setup-testnet.mjs`
  を順に走らせ、Testnet explorer で HQ SA contract と issued stamp を確認
- Task 006/007 で `.env.testnet` を CF Pages env vars に同期しデプロイ
